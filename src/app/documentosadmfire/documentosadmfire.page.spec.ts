import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DocumentosadmfirePage } from './documentosadmfire.page';

describe('DocumentosadmfirePage', () => {
  let component: DocumentosadmfirePage;
  let fixture: ComponentFixture<DocumentosadmfirePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DocumentosadmfirePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
