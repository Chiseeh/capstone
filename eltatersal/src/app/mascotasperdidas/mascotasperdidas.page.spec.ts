import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MascotasperdidasPage } from './mascotasperdidas.page';

describe('MascotasperdidasPage', () => {
  let component: MascotasperdidasPage;
  let fixture: ComponentFixture<MascotasperdidasPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MascotasperdidasPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
